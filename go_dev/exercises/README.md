# Exercises

One directory per exercise: `NN-short-name/`. Each is its own Go module — `go mod init` is part of the practice. Your attempt lives here; review happens on this code, then the topic's status updates.

Conventions:
- Module path `learning/NN-short-name` unless the exercise says otherwise.
- Run `go fmt ./...` and `go vet ./...` before asking for review.
- Keep solutions after review — later exercises sometimes build on them.

## Index
- `01-hello-module/` — Foundations §1 (issued 2026-07-25)
