# PARKED (2026-07-31 — Less-Is-More program)

This project (including `go_concurrency/`) is parked by the program's stack bet: **PHP/Laravel + fintech spine, TypeScript second stack — Go deliberately not pursued** through Jul 2028. See root `PROGRAM.md`.

**Re-entry trigger:** a deliberate stack re-bet after Jul 2028 (a `SOMEDAY.md` phase-boundary decision, not an impulse). Parked, not killed, because the bet is revisitable. Do not schedule sessions here.
