# Track 2 — Navigation

Goal: reach any file, symbol, or line in an unfamiliar repo in a couple of seconds.

| # | Topic | Status |
|---|-------|--------|
| 2.1 | Buffers vs windows vs tabs — the three things everyone conflates | pending |
| 2.2 | The picker (`<leader><space>`, `<leader>ff`, `<leader>fb`): files, buffers, recent | pending |
| 2.3 | Live grep `<leader>/` and `<leader>sg`; ripgrep flags that matter | pending |
| 2.4 | Window splits, resize, `<C-w>` vs LazyVim's `<C-h/j/k/l>` | pending |
| 2.5 | Jumps `<C-o>/<C-i>`, changelist `g;`, marks, `''` | pending |
| 2.6 | flash.nvim (`s`) — LazyVim's motion accelerator | pending |
| 2.7 | Explorer (neo-tree / snacks explorer) and when *not* to use a file tree | pending |
| 2.8 | Quickfix + loclist: the buffer that turns search results into a worklist | pending |

## Notes
_(none yet)_
