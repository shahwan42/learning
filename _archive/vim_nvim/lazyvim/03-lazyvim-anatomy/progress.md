# Track 3 — LazyVim anatomy

Goal: read LazyVim's own source and bend any part of it without guessing.

| # | Topic | Status |
|---|-------|--------|
| 3.1 | What a distro *is*: `init.lua` → lazy.nvim → LazyVim as a plugin exporting specs | pending |
| 3.2 | Load order and the `lua/config/` contract (`options`, `keymaps`, `autocmds`, `lazy`) | pending |
| 3.3 | Plugin specs: `opts` table vs `opts` function vs `config`; the merge rules | pending |
| 3.4 | Overriding vs replacing: `opts`, `keys`, `enabled=false`, `optional=true` | pending |
| 3.5 | Extras — `:LazyExtras`, `lazyvim.json`, and what an extra actually injects | pending |
| 3.6 | Lazy-loading triggers: `event`, `ft`, `cmd`, `keys` — and how they cost startup | pending |
| 3.7 | `lazy-lock.json`, `:Lazy update`, pinning, and recovering a broken update | pending |
| 3.8 | Lua you actually need: tables, `require`, `vim.opt`, `vim.keymap.set` | pending |

## Notes
_(none yet)_
