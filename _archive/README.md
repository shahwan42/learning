# _archive — killed curricula (2026-07-31 program cut)

Moved here by the Less-Is-More program (see `../PROGRAM.md`). Nothing was deleted; each of these is regenerable in an afternoon if ever truly needed — which is exactly why keeping them active was pure collecting.

| Dir | Why killed |
|---|---|
| `vim_nvim/` | Same-day duplicate of `terminal-based/neovim` (49 topics vs the 16 that live in the active project). Superseded within hours of creation, zero sessions ever run |
| `njs_ts--design_patterns/` | GoF-by-catalog: the lowest-yield ~20h in the inventory. Design judgment comes from the Ousterhout book track + capstone code reviews; patterns get learned on contact |
| `nodejs_from_scratch/` | Pre-system scratch files from a video course; no curriculum, no gates. Its subject matter is owned by `njs_ts/study-project` |

Do not resurrect anything here without going through `SOMEDAY.md` and Law 1 (kill-to-create pricing).
