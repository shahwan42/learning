import { computed, ref, type Ref } from 'vue';

const FEE_RATE = 0.005; // 0.5%
const MAX_FEE_HALALAS = 500;

export function usePaymentSummary(balanceHalalas: Ref<number>) {
    // TODO: your attempt

    return {
        // amountInput,
        // amountHalalas,
        // feeHalalas,
        // totalHalalas,
        // isValid,
        // reset,
    };
}

// Part B
// 1.
// 2.
