# MERGED into ledger-capstone (2026-07-31 — Less-Is-More program)

Do not run this project standalone. Its build spine (JS→TS double-entry ledger, CLI→HTTP, wallets/payouts/POS recon) is consumed by `../ledger-capstone/` — scope contract in `../ledger-capstone/README.md`, first session Aug 2027.

Dead by canonical-source law (root `PROGRAM.md`, Law 6): the `accounting/` track (owner: `fintech_payments/accounting_for_spwe`) and the `node/` + `typescript/` tracks (owner: `njs_ts/study-project`).
