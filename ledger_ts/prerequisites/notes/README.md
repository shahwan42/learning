# Prerequisites

Things taught just-in-time because an exercise needed them, not because they belonged to a
track. Kept here so a returning session can find them.

- Node basics (running a file, ESM, `node:test`) → [node/notes/01-running-node.md](../../node/notes/01-running-node.md)
