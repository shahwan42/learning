// Plan: <one line — how does the brute force find the pair?>
// Time: O(?)   Space: O(?)

/**
 * @param {number[]} nums
 * @param {number} target
 * @returns {number[]} indices of the two numbers that sum to target
 */
export function twoSum(nums, target) {
  // TODO
}
