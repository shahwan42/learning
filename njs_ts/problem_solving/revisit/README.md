# Revisit — parked topics

Deferred on purpose. Pull one in when its trigger shows up.

| Topic | Why it matters / when to pull it in |
|---|---|
| Monotonic stack / deque | "next greater element", sliding-window max. After stack + sliding window. |
| Union-Find (DSU) | Dynamic connectivity, "accounts merge". After graphs. |
| Tries | Prefix search, word games. Only if string-heavy interviews expected. |
| Bit manipulation | XOR tricks, subsets via masks. Cheap wins; JS bitwise is 32-bit signed — pairs with foundations #7. |
| Advanced DP (knapsack, LCS, edit distance) | Senior-interview differentiators. After DP 9–10 feel easy. |
| Segment tree / Fenwick | Range queries with updates. Rarely asked; enough to know they exist. |
| KMP / string matching | `indexOf` usually suffices; learn only if built-ins are banned. |
| Matrix tricks (rotate, spiral, in-place marking) | Common mediums; slot in after patterns-linear. |
| Randomized (shuffle, reservoir sampling) | Occasional; fun math. |
| V8 perf internals (elements kinds, hidden classes) | Why same-big-O solutions differ 10×. Curiosity fuel after foundations #2 benchmarking. |
| BigInt costs | For when >2⁵³ problems appear. |
