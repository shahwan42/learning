# Track: typescript

Enough type-system fluency to write and review production TS without fighting it.

Legend: ☐ not started · ▶ in progress · ✅ done

1. ☐ Mental model — types are erased at runtime; structural typing; TS ≠ runtime safety
2. ☐ tsconfig & strict mode — the handful of flags that matter
3. ☐ Everyday types & narrowing — unions, literal types, type guards, discriminated unions
4. ☐ Functions & generics — practical generics; when you're overdoing it
5. ☐ unknown vs any, assertions — the honesty spectrum; handling external data
6. ☐ Utility types — Partial, Pick, Omit, Record, ReturnType; deriving types from values
7. ☐ Validation at boundaries — zod: parse, don't trust; types from schemas
8. ☐ Declaration files & @types — consuming untyped packages
9. ☐ Running & building TS — Node's native type stripping, tsx, tsc as checker, bundlers (tsup/esbuild)

## Exercises
(none yet)
