# Track B — Building LLM features

From zero API experience to shipping AI inside a real product. Python for labs, Laravel for product wiring.

| # | Topic | Status | Why it matters |
|---|---|---|---|
| B1 | First API calls — Messages API from Python & Laravel; roles, system prompts, tokens & pricing, streaming, retries/timeouts | ⬜ | Wire-level fluency everything else builds on |
| B2 | Structured output — JSON/tool-schema outputs, few-shot, task decomposition; prompts as versioned artifacts | ⬜ | LLM output your code can actually consume |
| B3 | Tool calling & the agent loop — hand-write the loop; pipelines vs agents | ⬜ | Demystifies "agents" — it's a while loop around the API |
| B4 | Embeddings & retrieval — vectors, similarity, chunking; small pgvector lab | ⬜ | The other half of most AI features: search & memory |
| B5 | RAG end-to-end — pipeline over Foodics-shaped data; hybrid search; citations; RAG vs fine-tuning | ⬜ | The most-shipped AI feature shape in SaaS |
| B6 | Conversation state & caching — multi-turn memory, summarization, prompt caching | ⬜ | Multi-turn features live or die on state and cost |
| B7 | AI UX patterns — streaming, partial results, human-in-the-loop, failure states | ⬜ | AI fails visibly; UX is the mitigation layer |
