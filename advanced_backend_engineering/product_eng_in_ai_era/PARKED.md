# CONDITIONAL — parked unless the work initiative lands (2026-07-31 — Less-Is-More program)

This project activates (~6 units: evals, prompt design, LLM-feature engineering) **only if the already-pitched Claude-API initiative at work is approved** — and then it runs attached to that real deliverable as the H1-2027 visible initiative, not as a standalone course. See root `PROGRAM.md`.

If the pitch hasn't landed by the P2 boundary (Jul 2027), this parks permanently (re-entry via `SOMEDAY.md` only). Do not schedule speculative sessions here.
