# Exercises

One file per exercise, named `<topic-id>-<slug>.md` (e.g. `A1-end-to-end-map.md`). The file records the task; your attempt and the review happen in chat. Attempt before peeking at anything — wrong attempts teach more than clean ones.
